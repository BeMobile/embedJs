---
title: 'LlamaCpp'
---

EmbedJs supports local embeddings via `LlamaCpp`.

## Install LlamaCpp addon

```bash
npm install @betalent/embedjs-llama-cpp
```

## Usage

```ts
import { RAGApplicationBuilder } from '@betalent/embedjs';
import { LlamaCppEmbeddings } from '@betalent/embedjs-llama-cpp';

const app = await new RAGApplicationBuilder()
.setEmbeddingModel(new LlamaCppEmbeddings({
    modelPath: "./models/nomic-embed-text-v1.5.f16.gguf",
}))
```
