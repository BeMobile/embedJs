---
title: '📝 Markdown / MDX file'
---

You can load any Markdown(MD) or MDX file from your local file system or through a URL.

## Install Markdown addon

```bash
npm install @betalent/embedjs-loader-markdown
```

## Usage

```ts
import { RAGApplicationBuilder } from '@betalent/embedjs';
import { OpenAiEmbeddings } from '@betalent/embedjs-openai';
import { HNSWDb } from '@betalent/embedjs-hnswlib';
import { MarkdownLoader } from '@betalent/embedjs-loader-markdown';

const app = await new RAGApplicationBuilder()
.setModel(SIMPLE_MODELS.OPENAI_GPT4_O)
.setEmbeddingModel(new OpenAiEmbeddings())
.setVectorDatabase(new HNSWDb())
.build();

app.addLoader(new MarkdownLoader({ filePathOrUrl: '...' }))
```
