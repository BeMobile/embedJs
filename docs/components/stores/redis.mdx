---
title: Redis
---

You can use redis as a data store.

## Install Redis addon

```bash
npm install @betalent/embedjs-redis
```

## Usage
<CodeGroup>
```ts Partial example
import { RAGApplicationBuilder } from '@betalent/embedjs';
import { RedisStore } from '@betalent/embedjs-redis';

const app = await new RAGApplicationBuilder()
.setStore(new RedisStore({ ... }))
```
</CodeGroup>

<Snippet file="missing-store-tip.mdx" />
