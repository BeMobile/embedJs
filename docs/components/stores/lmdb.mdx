---
title: LMDB
---

You can use [LMDB](https://dbdb.io/db/lmdb) to store values locally on disk.

## Install LMDB addon

```bash
npm install @betalent/embedjs-lmdb
```

## Usage
<CodeGroup>
```ts Partial example
import { RAGApplicationBuilder } from '@betalent/embedjs';
import { LmdbStore } from '@betalent/embedjs-lmdb';

const app = await new RAGApplicationBuilder()
.setStore(new LmdbStore({ path: path.resolve('./store') }))
```
</CodeGroup>

<Snippet file="missing-store-tip.mdx" />
